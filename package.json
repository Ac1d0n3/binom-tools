{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build && node scripts/rewrite-build-asset-urls.mjs",
        "build:local": "npm run htaccess:local && npm run build",
        "build:production": "npm run build",
        "deploy:ftp": "node scripts/pack-ftp-deploy.mjs",
        "htaccess:local": "cp public/.htaccess.local public/.htaccess",
        "htaccess:production": "cp public/.htaccess.production public/.htaccess",
        "dev": "vite",
        "test": "vitest run",
        "test:watch": "vitest",
        "test:e2e": "playwright test",
        "validate:bundles": "node scripts/validate-tool-bundles.mjs"
    },
    "dependencies": {
        "@binom/sdk-governance": "file:packages/sdk-governance"
    },
    "devDependencies": {
        "@fortawesome/fontawesome-free": "^7.3.0",
        "@playwright/test": "^1.52.0",
        "@tailwindcss/vite": "^4.0.0",
        "concurrently": "^9.0.1",
        "jsdom": "^29.1.1",
        "laravel-vite-plugin": "^3.1",
        "prismjs": "^1.30.0",
        "tailwindcss": "^4.0.0",
        "vite": "^8.0.0",
        "vitest": "^3.2.4"
    }
}
